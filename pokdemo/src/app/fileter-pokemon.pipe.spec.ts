import { FilterPokemonPipe } from './fileter-pokemon.pipe';

describe('FileterPokemonPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPokemonPipe();
    expect(pipe).toBeTruthy();
  });
});
